package com.tuapp.tiendaadaptativa.data.database.dao

///**
// * DAO - OFERTAS
// * Fase: Persistencia (data)
// * Responsabilidad:
// * - Obtener ofertas activas
// * - Obtener ofertas por emoción (qué ofertas funcionan para cada emoción)
// * - Obtener ofertas por categoría de producto
// * - Actualizar estado de una oferta (activa/inactiva)
// */
interface OfferDao {
    // TODO: @Query("SELECT * FROM offers WHERE active = 1") suspend fun getActiveOffers(): List<Offer>
    // TODO: @Query("SELECT * FROM offers WHERE emotion = :emotion") suspend fun getByEmotion(emotion: String): List<Offer>
    // TODO: @Query("SELECT * FROM offers WHERE category = :category") suspend fun getByCategory(category: String): List<Offer>
    // TODO: @Update suspend fun update(offer: Offer)
}
