package com.tuapp.tiendaadaptativa.data.database.dao

///**
// * DAO - PRODUCTOS
// * Fase: Persistencia (data)
// * Responsabilidad:
// * - Obtener todos los productos del catálogo
// * - Buscar productos por categoría
// * - Buscar productos por rango de precio
// * - Obtener un producto específico por ID
// */
interface ProductDao {
    // TODO: @Query("SELECT * FROM products") suspend fun getAll(): List<Product>
    // TODO: @Query("SELECT * FROM products WHERE category = :category") suspend fun getByCategory(category: String): List<Product>
    // TODO: @Query("SELECT * FROM products WHERE price BETWEEN :min AND :max") suspend fun getByPriceRange(min: Double, max: Double): List<Product>
    // TODO: @Query("SELECT * FROM products WHERE id = :id") suspend fun getById(id: Int): Product?
}
