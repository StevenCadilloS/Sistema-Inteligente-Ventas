package com.tuapp.tiendaadaptativa.data.database.dao

///**
// * DAO - USUARIOS
// * Fase: Persistencia (data)
// * Responsabilidad:
// * - Insertar usuario nuevo (registro)
// * - Buscar usuario por email/password (login)
// * - Obtener usuario actual por ID
// */
interface UserDao {
    // TODO: @Insert suspend fun insert(user: User)
    // TODO: @Query("SELECT * FROM users WHERE email = :email") suspend fun findByEmail(email: String): User?
    // TODO: @Query("SELECT * FROM users WHERE id = :id") suspend fun findById(id: Int): User?
}
