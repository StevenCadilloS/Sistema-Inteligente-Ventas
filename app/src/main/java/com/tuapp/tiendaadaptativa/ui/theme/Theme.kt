package com.tuapp.tiendaadaptativa.ui.theme

///**
// * TEMA VISUAL ADAPTATIVO
// * Fase: UI (Frontend)
// * Responsabilidad:
// * - Definir esquema de colores base de la app
// * - Cambiar tema visual según emoción detectada en tiempo real
// * - Cada emoción tiene su propia paleta de colores:
// *     triste → fondo cálido beige, acentos azules (empatía, reconfortar)
// *     sorpresa → fondo claro blanco, acentos naranjas (energía, atención)
// *     felicidad → fondo elegante crema, acentos dorados (premium, lujo)
// *     neutral → tema por defecto de la tienda
// * - Se aplica globalmente desde ProductDetailActivity
// *
// * Propósito: el tema refuerza emocionalmente la oferta adaptativa
// */
class AdaptiveTheme {
    // TODO: fun getColorScheme(emotion: String): ColorScheme
    //       -> Retorna esquema de colores según emoción
    // TODO: private val sadTheme = lightColorScheme(
    //       primary = Color(0xFF4A90D9),      // Azul cálido
    //       background = Color(0xFFFFF8E1),    // Beige suave
    //       surface = Color(0xFFFFFFFF),
    //       onPrimary = Color.White
    //   )
    // TODO: private val surprisedTheme = lightColorScheme(
    //       primary = Color(0xFFFF9800),       // Naranja brillante
    //       background = Color(0xFFF5F5F5),    // Gris muy claro
    //       surface = Color(0xFFFFFFFF),
    //       onPrimary = Color.White
    //   )
    // TODO: private val happyTheme = lightColorScheme(
    //       primary = Color(0xFFFFD700),       // Dorado elegante
    //       background = Color(0xFFFFFBF0),    // Crema cálido
    //       surface = Color(0xFFFFFFFF),
    //       onPrimary = Color(0xFF333333)      // Texto oscuro sobre dorado
    //   )
    // TODO: private val neutralTheme = lightColorScheme(
    //       primary = Color(0xFF2196F3),       // Azul estándar
    //       background = Color(0xFFFFFFFF),    // Blanco
    //       surface = Color(0xFFF5F5F5),
    //       onPrimary = Color.White
    //   )
}
